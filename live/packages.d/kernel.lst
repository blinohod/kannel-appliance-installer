# ядро ставится только для того, чтобы взять от него модули
# само ядро используется только syslinux/isolinux, и не нужно
# внутри образа -- поэтому будет потом удалено
#
# сюда есть смысл добавлять модули, которые нужны при установке
# (т.е. все что нужно для дисков в нынешней версии инсталлятора)
kernel-image-std-def

# для тестирования внутри VirtualBox штука полезная
#kernel-modules-virtualbox-std-def
#kernel-modules-virtualbox-addition-std-def

